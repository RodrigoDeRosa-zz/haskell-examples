-- Types are little labels that values carry so that we
-- can reason about the values. But types have their own
-- little labels, called kinds. A kind is more or less
-- the type of a type. This may sound a bit weird and
-- confusing, but it's actually a really cool concept.

-- http://learnyouahaskell.com/making-our-own-types-and-typeclasses
-- Look at the end of that if you are interested